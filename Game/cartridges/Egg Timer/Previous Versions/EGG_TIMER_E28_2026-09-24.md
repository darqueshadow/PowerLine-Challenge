# E28: Chat's handoff and its additions, verbatim (2026-09-24)

Pasted to Code by Andrew. Filed unchanged; the packet merge is tagged *(E28)*.

## The ruling

```
E28 (RULED) — amends E27. Instructions move beside their objects.

1. KEEP the name "Time Warp" (undo the rename if already built).
   Under the sign, a small caption: "All clocks 5× fast. Get your
   next RCAV ready!" When it kicks in, the sign flashes 3 times
   (max 2/sec, same guard as the lights), then stays lit. Under
   reduced motion: lit, no flash. Grandfather clock with spinning
   hands stays as ruled in E27. How To Play step 5 reads "Time Warp!"
   instead of "Time Accelerator!".
2. Hose: add a small sink with a drain beside the spigot (placeholder
   art now; add a sink slot to the Gemini brief). Move the hose
   instructions onto it: "CLEANING HOSE: click & drag to spray. Use
   it any time!"
3. Cleanup banner between waves reads: "CLEAN-UP TIME! Hose down the
   mess before the next wave."
4. Command Lines: grey hint text in an empty line, "RCAV + unit",
   gone as soon as the player types. Put the Tab / F12 hints just
   under the lines.
5. Wave 1 only, then never again that game: a small tag on the first
   egg to go bold ("Ready! Type RCAV <unit>"), and a tag on the first
   "Clear @" note pointing to the wall clock ("Check the wall clock").
   No flashing on either.
6. Side instructions panel: remove whatever is now shown beside its
   object. Report what's left, and propose whether the panel is
   still needed.
7. Check everything fits at all four sizes, without covering nests
   or readouts. Rigs, then push.
```

## The additions (sent while it was being built)

```
E28 additions:
8. Command Line picker defaults to 2 (1–4 still selectable).
9. Use the colour cue for RCAV timing:
   - How To Play step 2: "Wait for pink. When the timer turns pink
     and bold, the egg is ready. Type RCAV + the unit (like RCAV 2101)
     and press Enter. Too early won't work."
   - Wave 1 tag on the first ready egg: "Pink = ready! Type RCAV <unit>"
```

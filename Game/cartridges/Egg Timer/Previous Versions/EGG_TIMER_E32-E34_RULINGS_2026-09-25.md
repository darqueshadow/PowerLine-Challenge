# Chat rulings E32–E34 (2026-09-25), verbatim

Pasted to Code by Andrew.

```
Chat rulings E32–E34 (Egg Timer)

E32 — Board tint: LILAC (as built). No change.

E33 — Bold timer: darken to #d1006a (white text, 5.4:1).
- Change the "ready pink" everywhere it means "this egg is ready / act now",
  not just the timer box: e.g. the wave-1 tag "Pink = ready!", the How To Play
  comic panel, any other readout that uses the same token. The player is taught
  "pink = act now", so every instance must match the real timer.
- Pink used purely as decoration (if any) stays as is. If you're unsure whether
  a use is "ready" or decoration, list it in your report rather than guessing.
- If this pink appears in "Egg Timer look for the NB cabinet.md", update that
  note too so NB copies the new value by hand.

E34 — Game Over default: TITLE SCREEN picked first (as built).
- Add: Game Over ignores Enter until ~1 s after it appears (or until Enter has
  been released and pressed fresh — your pick, whichever fits the existing
  Enter-before-load guard). Purpose: a player hammering Enter at the end of a
  game sees their result before leaving. Arrow keys and mute unaffected.
- The music-end auto-return to title is unchanged.

Standing rule applies: both rigs pass → one commit per item → push → one-line
report. Pilot art stays on the hold branch until Andrew OKs the updated sheet.
```
